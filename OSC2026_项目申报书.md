# moon_api_guard 项目申报书

## 基本信息

- 项目名称：moon_api_guard：MoonBit API 兼容性守卫工具
- 参赛者：TODO
- 联系方式：TODO
- GitHub 仓库链接：TODO
- GitLink 仓库链接：TODO
- 项目方向：MoonBit 工程基础设施 / API 兼容性与发布守卫工具
- 项目类型：原创项目，参考通用 API 兼容性检查思想，不直接移植特定开源项目

## 项目简介

`moon_api_guard` 计划为 MoonBit 生态提供一个边界清晰、可测试、可复用的公共 API 兼容性守卫工具。项目基于 `moon info` 生成的 `.mbti` 接口描述，对比旧版和新版公开接口，识别删除 API、函数签名变化、类型/trait/alias 变化等破坏性变更，区分兼容新增，并给出 SemVer 升级建议。它面向 mooncakes.io 发布前检查、PR API 审查、团队 CI 守卫、OSC 项目验收前自查等场景，帮助 MoonBit 包作者避免无意破坏下游用户。

## 核心功能范围

- 提供 `ApiItem`、`ApiChange`、`ApiReport` 等核心数据结构；
- 解析和规范化 `pkg.generated.mbti` 中的函数、类型、结构体、枚举、trait、impl 和 alias；
- 检测 API 删除、参数或返回类型变化、公开性收紧等破坏性变更；
- 检测新增 public API 等兼容变更；
- 将变更分类为 breaking / compatible / informational / unknown；
- 根据变更类型给出 major / minor / patch 的 SemVer 建议；
- 输出终端摘要、Markdown 报告和 JSON 报告；
- 提供 `moon_api_guard check old.mbti new.mbti` 命令和 GitHub Actions / GitLink CI 示例，发现破坏性变更时返回非零退出码。

## 实现计划

- 2026-07-12：完成仓库改名、MoonBit 项目骨架、README、LICENSE、基础 API diff 数据结构和第一批测试；
- 2026-07-13：完成 `.mbti` 轻量解析、函数/类型/结构体/枚举/trait/alias 的基础差异分类；
- 2026-07-14：完成 SemVer 建议、终端摘要、Markdown / JSON 报告；
- 2026-07-15：补充 CLI、示例项目、GitHub Actions / GitLink CI 模板；
- 2026-07-16 至 2026-07-17：根据验收反馈补充测试、发布 mooncakes.io、完善 README 和工程质量。

## 开源与合规说明

本项目采用 Apache-2.0 License。项目为原创 MoonBit 实现，不复制第三方项目源码；概念上参考 Rust `cargo-semver-checks`、Java `japicmp` / `revapi` 等 API 兼容性检查工具，但输入格式、规则分类、CI 集成和发布流程面向 MoonBit `.mbti` 与 mooncakes.io 重新设计。当前公开查重未发现成熟同名或高度相似的 MoonBit 专用 API 兼容性守卫工具；该结论仅基于 GitHub、mooncakes.io、GitLink 和公开网页搜索，不能绝对排除未公开项目。
